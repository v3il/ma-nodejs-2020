const nodeManager = require('./node');
const axiosManager = require('./axios');
const requestManager = require('./request');

module.exports = {
    nodeManager,
    axiosManager,
    requestManager,
};
