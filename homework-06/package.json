{
    "name": "homework-06",
    "main": "src/index.js",
    "scripts": {
        "dev": "set RATE=1000 && set LIMIT=100 && set COLOR=true && nodemon ./src/index.js --rate=500 --limit=100",
        "start": "set RATE=1000 && set LIMIT=100 && set COLOR=true && node ./src/index.js --rate=500 --limit=100",
        "lint": "npx eslint --ignore-path .gitignore .",
        "lint:fix": "npx eslint --ignore-path .gitignore . --fix"
    },
    "author": "Dmitry Kit",
    "devDependencies": {
        "eslint": "^6.6.0",
        "eslint-config-airbnb-base": "^14.0.0",
        "eslint-config-prettier": "^6.5.0",
        "eslint-plugin-import": "^2.18.2",
        "eslint-plugin-prettier": "^3.1.1",
        "husky": "^3.0.9",
        "nodemon": "^2.0.2",
        "prettier": "^1.19.1"
    },
    "husky": {
        "hooks": {
            "pre-commit": "npm run lint:fix && cd .. && git add ."
        }
    }
}
